<!DOCTYPE html>
<html lang="en">

<head>
    <script>
        (function(){try{var c=document.cookie.match(/(?:^|; )wg_household_code=([^;]*)/);var u=document.cookie.match(/(?:^|; )wg_member_username=([^;]*)/);var code=c?decodeURIComponent(c[1]).trim():'';var user=u?decodeURIComponent(u[1]).trim():'';if(code&&user&&(location.pathname==='/'||location.pathname==='/index.html')){location.replace('/household/'+encodeURIComponent(code));}}catch(e){}})();
    </script>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <meta name="theme-color" content="#0f172a" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <meta name="apple-mobile-web-app-title" content="WG-Chores" />
    <base href="/" />
    <link rel="manifest" href="manifest.json" />
    <link rel="apple-touch-icon" href="favicon.png" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700;1,9..40,400&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="bootstrap/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link rel="stylesheet" href="app.css" />
    <link rel="stylesheet" href="WG-Chores.styles.css" />
    <link rel="icon" type="image/png" href="favicon.png" />
    <HeadOutlet @rendermode="InteractiveServer" />
</head>

<body>
    <div id="components-reconnect-modal" class="components-reconnect-hide">
        <div class="reconnect-overlay">
            <div class="reconnect-card reconnect-show">
                <div class="reconnect-spinner" aria-hidden="true"></div>
                <p class="reconnect-title">You're offline</p>
                <p class="reconnect-message">Reconnecting…</p>
            </div>
            <div class="reconnect-card reconnect-failed">
                <i class="fa-solid fa-wifi fa-2x reconnect-icon" aria-hidden="true"></i>
                <p class="reconnect-title">Connection lost</p>
                <p class="reconnect-message">We couldn't reach the server. Check your connection and try again.</p>
                <button type="button" class="btn btn-primary reconnect-btn" onclick="if(window.Blazor)window.Blazor.reconnect()">Try again</button>
            </div>
            <div class="reconnect-card reconnect-rejected">
                <i class="fa-solid fa-arrow-rotate-right fa-2x reconnect-icon" aria-hidden="true"></i>
                <p class="reconnect-title">Session expired</p>
                <p class="reconnect-message">Your session was lost. Reload the page to continue.</p>
                <button type="button" class="btn btn-primary reconnect-btn" onclick="location.reload()">Reload</button>
            </div>
        </div>
    </div>
    <Routes @rendermode="InteractiveServer" />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="js/site.js"></script>
    <script src="_framework/blazor.web.js"></script>
</body>

</html>
